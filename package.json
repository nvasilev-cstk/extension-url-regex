{
  "name": "url-field-extension",
  "version": "1.0.0",
  "description": "Contentstack custom field extension — validated URL field that mirrors an OOB URL field",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@contentstack/app-sdk": "^1.3.6"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.3.1"
  }
}
